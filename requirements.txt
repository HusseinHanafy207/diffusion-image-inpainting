# Core (match generative-models; install that package separately as editable)
torch>=2.0
torchvision>=0.15
numpy>=1.24
matplotlib>=3.7
tqdm>=4.65
pyyaml>=6.0
scikit-image>=0.22
Pillow>=10.0

# Install sibling DDPM package first, e.g.:
#   pip install -e ../../generative-models
# or from an absolute path to my generative-models checkout.
