# Core (match generative-models; install that package separately as editable)
torch>=2.0
torchvision>=0.15
numpy>=1.24
matplotlib>=3.7
tqdm>=4.65
pyyaml>=6.0
scikit-image>=0.22
Pillow>=10.0

# Install generative-models first (editable), e.g.:
#   pip install -e /path/to/generative-models
# Repo: https://github.com/HusseinHanafy207/generative-models
